@model Kurs
<p>
    <b>@Model.Baslik </b> adlı kursu silmek istediğinize emin misiniz?
    <hr>
    <form asp-action="Delete" method="post">
        <input type="hidden" name="Id" value="@Model.KursId">
        <button type="submit" class="btn btn-sm btn-danger">Kursu Sil</button>
        <a asp-action="Index" class="btn btn-sm btn-primary">Vazgeç</a>
    </form>
</p>